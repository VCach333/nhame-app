import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  
    themeContentItem: {
        gap: 6,
        alignItems: 'center',
    },
    themeIcon: {
        width: 24,
        height: 24
    },
    themeText: {
        fontSize: 12,
        fontWeight: 500
    },
})

export default styles